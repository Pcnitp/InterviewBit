public class Solution {
    public String reverseWords(String a) {
        String[] s=a.split(" ");
        int n=s.length;
        String ans="";
        for(int i=n-1;i>=0;i--)
            ans+=s[i]+" ";
        ans=ans.substring(0,ans.length()-1);
        return ans;
    }
}
