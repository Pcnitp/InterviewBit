public class Solution {
    public int mice(int[] a, int[] b) {
        Arrays.sort(a);
        Arrays.sort(b);
        int n=a.length;
        int ans=0;
        for(int i=0;i<n;i++)
            ans+=Math.abs(a[i]-b[i]);
        return ans;
    }
}
